import { AppBar, Toolbar } from '@mui/material';

export default function () {
	// component logic

	// component layout
	return (
		<AppBar color={'inherit'} position={'static'}>
			<Toolbar></Toolbar>
		</AppBar>
	);
}
