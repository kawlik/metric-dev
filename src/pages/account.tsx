import { AppPageEmpty, AppViewStack } from '../components/@';

export default function (props: {}) {
	// component logic

	// component layout
	return (
		<AppViewStack flex={1}>
			<AppPageEmpty />
		</AppViewStack>
	);
}
